<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib">
    
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/Components/Resource.Base.Color.xaml"/>
        <ResourceDictionary Source="/Components/Resource.Base.Value.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <Style TargetType="{x:Type Button}">
        <Setter Property="Foreground" Value="{StaticResource foregroundColor}"/>
        <Setter Property="FontSize" Value="{StaticResource ResourceKey=FontSize.Default}"/>
        <Setter Property="Margin" Value="{StaticResource ResourceKey=Margin.Default}" />
    </Style>

    <Style TargetType="{x:Type Button}" x:Key="StyleNumberButton">
        <Setter Property="Foreground" Value="{StaticResource foregroundColor}"/>
        <Setter Property="FontSize" Value="20"/>
        <Setter Property="Margin" Value="5"/>
        <Setter Property="Background" Value="{StaticResource numberColor}"/>
    </Style>

    <Style TargetType="Button" x:Key="StyleOperatorButton" BasedOn="{StaticResource StyleNumberButton}">
        <Setter Property="Background" Value="{StaticResource operatorColor}"/>
    </Style>

    <Style TargetType="Button" x:Key="StyleFunctionButton" BasedOn="{StaticResource StyleNumberButton}">
        <Setter Property="Background" Value="LightGray"/>
        <Setter Property="Foreground" Value="Black"/>
    </Style>

</ResourceDictionary>