<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework">

    <Style TargetType="Window" x:Key="CustomChromeWindowStyle">
        <Setter Property="shell:WindowChrome.WindowChrome">
            <Setter.Value>
                <shell:WindowChrome 
                    CaptionHeight="30"
                    ResizeBorderThickness="6"
                    CornerRadius="0"
                    GlassFrameThickness="0"
                    UseAeroCaptionButtons="False" />
            </Setter.Value>
        </Setter>

        <Setter Property="WindowStyle" Value="None" />
        <Setter Property="AllowsTransparency" Value="False" />
        <Setter Property="Background" Value="White" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Window">
                    <Grid>
                        <!-- Title Bar -->
                        <Grid Background="Gray" Height="30" VerticalAlignment="Top">
                            <TextBlock Text="{TemplateBinding Title}" 
                                       VerticalAlignment="Center" 
                                       Margin="10,0,0,0" 
                                       Foreground="White" />
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <!--<Button Content="_" Click="MinimizeClick" />
                                <Button Content="□" Click="MaximizeRestoreClick" />
                                <Button Content="X" Click="CloseClick" />-->
                                <Button Content="_" />
                                <Button Content="□" />
                                <Button Content="X" />
                            </StackPanel>
                        </Grid>

                        <!-- Content -->
                        <Border Margin="0,30,0,0">
                            <AdornerDecorator>
                                <ContentPresenter />
                            </AdornerDecorator>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>