<Window x:Class="WpfCoreApp.Mvvm.View.NewContactWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfCoreApp.Mvvm.View"
        xmlns:vm="clr-namespace:WpfCoreApp.Mvvm.ViewModel"
        mc:Ignorable="d"
        Title="NewContactWindow" Height="300" Width="300">
    <!--<Window.Resources>
        <vm:ContactsVM x:Key="vm"/>
    </Window.Resources>-->
    <Border BorderBrush="DarkGray" BorderThickness="2"><!--DataContext="{StaticResource vm}">-->
        <Grid>
            <StackPanel Margin="10">
                <Label Content="Name:"/>
                <TextBox x:Name="txtName" Text="{Binding Name,Mode=TwoWay}" Margin="0,0,0,5"/>
                <Label Content="Email:" />
                <TextBox x:Name="txtEmail" Text="{Binding Email,Mode=TwoWay}" Margin="0,0,0,5"/>
                <Label Content="Phone:"/>
                <TextBox x:Name="txtPhone" Text="{Binding PhoneNumber,Mode=TwoWay}" Margin="0,0,0,5"/>
                <Button Content="Save" Command="{Binding SubmitCommand}" CommandParameter="{Binding Name}" Margin="0,25,0,0"/>
                <Button Content="Cancel" Command="{Binding CloseCommand}" Margin="0,15,0,0"
                        CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>

            </StackPanel>
        </Grid>
    </Border>
</Window>
