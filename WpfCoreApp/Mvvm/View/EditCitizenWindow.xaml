<Window x:Class="WpfCoreApp.Mvvm.View.EditCitizenWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Citizen" Height="300" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBox Grid.Row="0" Text="{Binding Citizen.FirstName}" Margin="5" />
        <TextBox Grid.Row="1" Text="{Binding Citizen.LastName}" Margin="5" />
        <DatePicker Grid.Row="2" SelectedDate="{Binding Citizen.DateOfBirth}" Margin="5" />

        <!-- Save button -->
        <Button Grid.Row="3" Content="Save"
                Command="{Binding SaveCommand}" Margin="5"/>
    </Grid>
</Window>
