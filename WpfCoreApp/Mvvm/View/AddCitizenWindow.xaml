<Window x:Class="WpfCoreApp.Mvvm.View.AddCitizenWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
        xmlns:local="clr-namespace:WpfCoreApp.AppHelper"
        Title="Add Citizen" Height="300" Width="400">
    <Window.Resources>
        <!--<local:EmptyStringToVisibilityConverter x:Key="EmptyStringToVisibilityConverter"/>-->
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="First Name" VerticalAlignment="Center"/>
        <TextBox Grid.Row="0" Text="{Binding Citizen.FirstName}" Margin="85,5,5,5" />
        
        <!--Margin="10,0,0,0"
               Foreground="Gray"
               IsHitTestVisible="False"
               VerticalAlignment="Center"
               Visibility="{Binding Text, ElementName=FirstNameBox,
                            Converter={StaticResource EmptyStringToVisibilityConverter}}"/>-->

        <TextBlock Grid.Row="1" Text="Last Name" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Text="{Binding Citizen.LastName}" Margin="85,5,5,5" />
        
        <!--Margin="10,0,0,0"
           Foreground="Gray"
           IsHitTestVisible="False"
           VerticalAlignment="Center"
           Visibility="{Binding Text, ElementName=LastNameBox,
                        Converter={StaticResource EmptyStringToVisibilityConverter}}"/>-->
        <TextBlock Grid.Row="2" Text="Date Of Birth" VerticalAlignment="Center"/>
        <DatePicker Grid.Row="2" SelectedDate="{Binding Citizen.DateOfBirth}" Margin="85,5,5,5"/>

        <Button Grid.Row="3" Content="Save"
                Command="{Binding SaveCommand}" Margin="5"/>
    </Grid>
</Window>
